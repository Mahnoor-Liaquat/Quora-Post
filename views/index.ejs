<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Posts</title>
    <link rel="stylesheet" href="/style.css" />
</head>
<body>
    <div class="container">
        <h1>Quora Posts</h1>
        <% for(post of posts) { %>
        <div class="post">
            <h3 class="user">@<%= post.username %></h3>
            <h4 class="content"><%= post.content %></h4>
            <div class="actions">
                <a class="detail-link" href="http://localhost:8080/posts/<%=post.id%>">See in Detail</a>
                <a class="edit-link" href="http://localhost:8080/posts/<%=post.id%>/edit">Edit</a>
                <form action="/posts/<%= post.id%>?_method=DELETE" method="post" class="delete-form">
                    <button class="delete-button">Delete Post</button>
                </form>
            </div>
        </div>
        <% } %>
        <div class="new-post">
            <a href="http://localhost:8080/posts/new">Create New Post</a>
        </div>
    </div>
</body>
</html>
